# campus-copilot
Agent is a domain-agnostic conversational AI engine designed to bridge Generative AI models with relational databases using LLM Function Calling. Replacing static UI forms and rigid logic, it acts as an intelligent natural-language layer over structured data—enabling real-time database actions while maintaining strict authorization boundaries.
